import Link from "next/link";
import { ArrowRight, CircleUserRound, MapPin, Package, ShieldCheck } from "lucide-react";
import { OrderStatusBadge } from "@/components/ui/status-badge";
import { formatLkr } from "@/lib/formatting/currency";
import { mockAddresses, mockOrders } from "@/mocks/data";

export default function AccountPage() { const latest = mockOrders[0]; const address = mockAddresses[0]; return <div><section className="rounded-[2rem] bg-brand p-6 text-white sm:p-8"><p className="text-sm font-bold text-soft">Good afternoon, Ayesha</p><h2 className="mt-2 text-3xl font-black">Welcome back.</h2><p className="mt-3 max-w-xl text-sm leading-6 text-white/70">Your latest order is on its way. Everything else is right where you left it.</p><Link href="/account/orders" className="mt-6 inline-flex min-h-11 items-center gap-2 rounded-full bg-white px-5 text-sm font-black text-brand">View orders <ArrowRight className="size-4" /></Link></section><div className="mt-5 grid gap-4 md:grid-cols-3"><Link href="/account/orders" className="panel p-5 transition hover:-translate-y-0.5 hover:shadow-card-hover"><Package className="size-5 text-brand" /><p className="mt-4 text-xs font-bold text-ink-muted">Latest order</p><p className="mt-1 font-black text-brand">{latest.id}</p><div className="mt-3"><OrderStatusBadge status={latest.status} /></div></Link><Link href="/account/addresses" className="panel p-5 transition hover:-translate-y-0.5 hover:shadow-card-hover"><MapPin className="size-5 text-brand" /><p className="mt-4 text-xs font-bold text-ink-muted">Default delivery</p><p className="mt-1 font-black text-brand">{address.label}</p><p className="mt-2 text-xs leading-5 text-ink-muted">{address.summary}</p></Link><Link href="/account/profile" className="panel p-5 transition hover:-translate-y-0.5 hover:shadow-card-hover"><CircleUserRound className="size-5 text-brand" /><p className="mt-4 text-xs font-bold text-ink-muted">Profile</p><p className="mt-1 font-black text-brand">Almost complete</p><div className="mt-3 h-2 overflow-hidden rounded-full bg-soft/45"><span className="block h-full w-4/5 rounded-full bg-brand" /></div><p className="mt-2 text-xs text-ink-muted">Add a secondary phone number</p></Link></div><section className="panel mt-5 p-6"><div className="flex items-center justify-between gap-4"><div><p className="text-xs font-bold text-ink-muted">Recent order</p><h2 className="mt-1 text-xl font-black text-brand">{latest.id}</h2></div><OrderStatusBadge status={latest.status} /></div><div className="mt-5 grid gap-4 border-t border-border pt-5 text-sm sm:grid-cols-3"><div><p className="text-ink-muted">Placed</p><p className="mt-1 font-black text-brand">{latest.date}</p></div><div><p className="text-ink-muted">Items</p><p className="mt-1 font-black text-brand">{latest.itemCount}</p></div><div><p className="text-ink-muted">Total</p><p className="mt-1 font-black text-brand">{formatLkr(latest.total)}</p></div></div></section><div className="mt-5 flex items-start gap-3 rounded-2xl bg-soft/35 p-5"><ShieldCheck className="mt-0.5 size-5 shrink-0 text-brand" /><div><p className="text-sm font-black text-brand">Session security belongs to the backend</p><p className="mt-1 text-sm leading-6 text-ink-muted">This account area uses mock customer data until the authenticated profile endpoint is connected.</p></div></div></div>; }
