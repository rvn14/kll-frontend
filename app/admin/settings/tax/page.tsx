import { AlertTriangle, History, Percent, Save } from "lucide-react";
import { AdminPageHeader } from "@/components/admin/admin-page-header";
export default function TaxRatePage() { return <div><AdminPageHeader eyebrow="Settings / Tax" title="Tax rate" description="Review the value returned by the settings API before making a change." /><div className="grid gap-5 lg:grid-cols-[1fr_320px]"><section className="panel p-6 sm:p-8"><div className="flex items-center gap-3"><span className="flex size-11 items-center justify-center rounded-2xl bg-soft/50 text-brand"><Percent className="size-5" /></span><div><h2 className="font-black text-brand">Current tax rate</h2><p className="text-xs text-ink-muted">Mock value · backend not connected</p></div></div><label className="mt-7 block max-w-sm"><span className="field-label">Tax rate (%)</span><div className="relative"><input className="field pr-12 text-lg font-black" type="number" min="0" step="0.01" defaultValue="18" /><Percent className="absolute right-4 top-1/2 size-4 -translate-y-1/2 text-ink-muted" /></div></label><div className="mt-6 flex gap-3 rounded-2xl bg-amber-50 p-4"><AlertTriangle className="mt-0.5 size-5 shrink-0 text-amber-700" /><p className="text-xs leading-5 text-amber-900">Changing the tax rate affects backend calculations. Production must show a confirmation modal and use the authoritative value returned after update.</p></div><button className="mt-6 inline-flex min-h-12 items-center gap-2 rounded-full bg-brand px-6 text-sm font-black text-white"><Save className="size-4" />Review change</button></section><aside className="panel h-fit p-5"><History className="size-5 text-brand" /><h2 className="mt-4 font-black text-brand">Audit information</h2><p className="mt-2 text-sm leading-6 text-ink-muted">An audit trail will appear here if a supported endpoint is added later.</p><div className="mt-5 rounded-2xl bg-surface-muted p-4 text-xs text-ink-muted">No audit endpoint has been invented.</div></aside></div></div>; }
