import { notFound } from "next/navigation";
import { MapPin, PackageCheck, Save, UserRound } from "lucide-react";
import { AdminPageHeader } from "@/components/admin/admin-page-header";
import { OrderStatusBadge } from "@/components/ui/status-badge";
import { formatLkr } from "@/lib/formatting/currency";
import { mockOrders } from "@/mocks/data";
export default async function AdminOrderDetailPage({ params }: { params: Promise<{ orderId: string }> }) { const { orderId } = await params; const order = mockOrders.find((item) => item.id === orderId); if (!order) notFound(); return <div><AdminPageHeader eyebrow="Orders / Details" title={order.id} description="Review the order before submitting any status update." actions={<OrderStatusBadge status={order.status} />} /><div className="grid gap-5 xl:grid-cols-[1fr_340px]"><div className="space-y-5"><section className="panel p-6"><div className="flex items-center gap-3"><PackageCheck className="size-5 text-brand" /><h2 className="text-lg font-black text-brand">Item summary</h2></div><div className="mt-5 flex items-center justify-between gap-4 rounded-2xl bg-surface-muted p-4"><div><p className="font-black text-brand">VisionPro 55&quot; 4K Smart Television</p><p className="mt-1 text-xs text-ink-muted">KLL-TV-001 · Qty 1</p></div><p className="shrink-0 font-black text-brand">{formatLkr(order.total)}</p></div></section><section className="grid gap-5 sm:grid-cols-2"><div className="panel p-5"><UserRound className="size-5 text-brand" /><h2 className="mt-4 font-black text-brand">Customer</h2><p className="mt-2 text-sm text-ink-muted">Ayesha Perera<br />ayesha@example.com<br />+94 77 123 4567</p></div><div className="panel p-5"><MapPin className="size-5 text-brand" /><h2 className="mt-4 font-black text-brand">Delivery</h2><p className="mt-2 text-sm text-ink-muted">42 Lake Road<br />Colombo 05<br />Sri Lanka</p></div></section></div><aside className="panel h-fit p-6"><h2 className="text-lg font-black text-brand">Update status</h2><p className="mt-2 text-xs leading-5 text-ink-muted">A confirmation dialog is required before the production mutation is sent.</p><label className="mt-5 block"><span className="field-label">New status</span><select className="field" defaultValue={order.status}><option value="pending">Pending</option><option value="processing">Processing</option><option value="shipped">Shipped</option><option value="delivered">Delivered</option><option value="cancelled">Cancelled</option></select></label><label className="mt-4 block"><span className="field-label">Internal note</span><textarea className="field min-h-24" /></label><button className="mt-5 inline-flex min-h-12 w-full items-center justify-center gap-2 rounded-full bg-brand px-5 text-sm font-black text-white"><Save className="size-4" />Review update</button><p className="mt-4 text-center text-xs text-ink-muted">PATCH /api/v1/admin/orders/{order.id}/status</p></aside></div></div>; }
