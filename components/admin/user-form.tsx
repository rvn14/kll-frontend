import { Save, ShieldCheck } from "lucide-react";
import type { UserSummary } from "@/types";
export function UserForm({ user }: { user?: UserSummary }) { return <form className="panel max-w-3xl p-5 sm:p-7"><div className="grid gap-5 sm:grid-cols-2"><label><span className="field-label">Full name</span><input className="field" defaultValue={user?.name} /></label><label><span className="field-label">Email address</span><input type="email" className="field" defaultValue={user?.email} /></label><label><span className="field-label">Role</span><select className="field" defaultValue={user?.role ?? "customer"}><option value="customer">Customer</option><option value="admin">Admin</option></select></label><label><span className="field-label">Account status</span><select className="field" defaultValue={user?.active === false ? "inactive" : "active"}><option value="active">Active</option><option value="inactive">Inactive</option></select></label><label className="sm:col-span-2"><span className="field-label">Temporary password</span><input type="password" className="field" autoComplete="new-password" /><span className="mt-1 block text-xs text-ink-muted">Only used when the confirmed create-user contract requires one.</span></label></div><div className="mt-6 flex gap-3 rounded-2xl bg-soft/35 p-4"><ShieldCheck className="mt-0.5 size-5 shrink-0 text-brand" /><p className="text-xs leading-5 text-ink-muted">Displayed roles are not frontend security. The backend must authorize every admin action.</p></div><button type="button" className="mt-6 inline-flex min-h-12 items-center gap-2 rounded-full bg-brand px-6 text-sm font-black text-white"><Save className="size-4" />Save user</button></form>; }
