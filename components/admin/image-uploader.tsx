"use client";

import { FileImage, Trash2, UploadCloud } from "lucide-react";
import { useState } from "react";

export function ImageUploader() { const [files, setFiles] = useState<File[]>([]); return <div><label className="flex min-h-64 cursor-pointer flex-col items-center justify-center rounded-3xl border-2 border-dashed border-soft-strong bg-soft/20 p-8 text-center text-brand transition hover:bg-soft/35"><UploadCloud className="size-9" /><span className="mt-4 text-lg font-black">Drop product media here</span><span className="mt-2 text-sm text-ink-muted">or select PNG, JPG or WebP files</span><input type="file" accept="image/png,image/jpeg,image/webp" multiple className="sr-only" onChange={(event) => setFiles(Array.from(event.target.files ?? []))} /></label>{files.length > 0 && <div className="mt-5 space-y-3">{files.map((file) => <div className="flex items-center gap-3 rounded-2xl border border-border p-3" key={`${file.name}-${file.lastModified}`}><span className="flex size-11 items-center justify-center rounded-2xl bg-soft/45 text-brand"><FileImage className="size-5" /></span><div className="min-w-0 flex-1"><p className="truncate text-sm font-black text-brand">{file.name}</p><p className="mt-1 text-xs text-ink-muted">{Math.ceil(file.size / 1024)} KB · Ready to upload</p><div className="mt-2 h-1.5 overflow-hidden rounded-full bg-surface-muted"><span className="block h-full w-0 bg-brand" /></div></div><button onClick={() => setFiles((current) => current.filter((item) => item !== file))} className="flex size-11 items-center justify-center rounded-full text-red-700 hover:bg-red-50" aria-label={`Remove ${file.name}`}><Trash2 className="size-4" /></button></div>)}</div>}</div>; }
