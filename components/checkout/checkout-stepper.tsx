import { Check } from "lucide-react";

const steps = ["Delivery", "Payment", "Confirmation"];
export function CheckoutStepper({ current }: { current: 1 | 2 | 3 }) { return <ol className="mx-auto mb-9 flex max-w-2xl items-center" aria-label="Checkout progress">{steps.map((step, index) => { const number = index + 1; const active = number === current; const done = number < current; return <li key={step} className={`flex items-center ${index < steps.length - 1 ? "flex-1" : ""}`} aria-current={active ? "step" : undefined}><div className="flex flex-col items-center gap-2"><span className={`flex size-9 items-center justify-center rounded-full text-sm font-black ${active ? "bg-brand text-white ring-4 ring-soft/60" : done ? "bg-soft text-brand" : "bg-white text-ink-muted ring-1 ring-border"}`}>{done ? <Check className="size-4" /> : number}</span><span className={`text-[11px] font-bold sm:text-xs ${active || done ? "text-brand" : "text-ink-muted"}`}>{step}</span></div>{index < steps.length - 1 && <span className={`mx-2 mb-5 h-px flex-1 ${done ? "bg-brand" : "bg-border"}`} />}</li>; })}</ol>; }
